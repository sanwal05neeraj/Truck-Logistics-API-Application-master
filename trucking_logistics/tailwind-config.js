// tailwind.config.js
module.exports = {
  darkMode: "media", // Enable dark mode based on user's system preference
  theme: {
    extend: {
      backgroundColor: {
        dark: "#1d1d1d", // Background color for dark theme
      },
      textColor: {
        dark: "#e0e0e0", // Text color for dark theme
      },
    },
  },
  variants: {},
  plugins: [],
};
